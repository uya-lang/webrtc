// std.yaml.impl - ToYaml 接口（结构体由实现者写 to_yaml；标量用 encoder 的 yaml_write_*）
// 模块路径：lib/std/yaml/impl.uya → use std.yaml.impl;

use std.yaml.encoder.YamlWriter;

export interface ToYaml {
    fn to_yaml(self: &Self, w: &YamlWriter) void;
}
