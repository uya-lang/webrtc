// std.yaml - YAML 编解码器模块入口（目录即模块）
// 模块路径：lib/std/yaml/ → use std.yaml;
//
// Phase 1：Flow 解析 `parse(arena, ptr, len) !YamlValue`
// Phase 2：Flow 编码 `yaml_encode_to` / `yaml_encode`、与 JsonValue 互转 `yaml_value_to_json_value` 等
// 说明：`encode_to`/`encode` 与 std.json 同名合并会冲突，故 YAML 侧使用 `yaml_` 前缀。
