// lib/std/core/error.uya - 核心错误标签（对齐设计文档）

// 说明：项目目前在 `lib/std/error.uya` 已定义具体错误标签；
// 这里提供 `std.core.error` 的同名包装，便于后续按设计迁移。

error IOError;
error PermissionDenied;
error NotFound;
error InvalidArgument;
error UnexpectedEof;

