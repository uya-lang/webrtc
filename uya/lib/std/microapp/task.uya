// std.microapp.task - portable microapp task controls
// 模块路径：use std.microapp.task

use std.microapp.abi.MICROAPP_SYS_YIELD;

export fn yield_now() !void {
    _ = try @syscall(MICROAPP_SYS_YIELD, 0, 0);
}
