use std.collections.vec.Vec;
use std.collections.vec.vec_new;
use std.mem.allocator.IAllocator;

export fn vec_helper_new_byte(allocator: IAllocator) Vec<byte> {
    return vec_new<byte>(allocator);
}
