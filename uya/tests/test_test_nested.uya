// 嵌套块中的测试单元

use std.testing.assert_eq_i32;

test "in function" {
    const x: i32 = 42;
    try assert_eq_i32(x, 42);
}

test "in nested block" {
    const y: i32 = 100;
    try assert_eq_i32(y, 100);
}
