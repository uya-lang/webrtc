// 测试宏内简单插值

use std.testing.assert_eq_i32;
use std.testing.assert_eq_bool;
use std.testing.assert_true;

mc test_add(a: expr, b: expr) expr {
    @mc_code(@mc_ast(${a} + ${b}));
}

test "test_macro_interp" {
    const result: i32 = test_add(3, 5);
    try assert_eq_i32(result, 8);
}
