// defer 在 return 前执行

use std.testing.expect;

var defer_ran: i32 = 0;

fn f() i32 {
    defer { defer_ran = 1; }
    return 0;
}

test "defer before return" {
    defer_ran = 0;
    const r: i32 = f();
    try expect(defer_ran == 1);  // defer 应在 return 前执行
    try expect(r == 0);
}
