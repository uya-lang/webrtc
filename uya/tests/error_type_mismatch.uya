// 类型不匹配错误测试
// 预期：编译失败（类型不匹配）
// 注意：此文件预期编译失败，用于验证编译器的类型检查

fn main() i32 {
    var x: i32 = 10;
    var y: bool = true;
    
    // 类型不匹配：i32 和 bool 不能直接比较
    if x == y {  // 应该编译失败
        return 1;
    }
    
    return 0;
}

