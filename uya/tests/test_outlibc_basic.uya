// test_outlibc_basic.uya - 测试 --outlibc 功能
use std.runtime.entry;
// 此测试验证编译器能够生成独立的 libc 库文件
// 
// 测试步骤：
// 1. 使用 --outlibc 生成 libuya.c 和 libuya.h
// 2. 验证生成的文件存在
// 3. 验证生成的文件包含必要的类型定义和函数声明
//
// 注意：这是一个编译测试，实际生成验证由测试脚本完成

export fn main() i32 {
    // 这个测试文件本身只是验证编译器能正常工作
    // 真正的 --outlibc 功能测试在 shell 脚本中
    return 0;
}
