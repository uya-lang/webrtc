use std.runtime.entry;

fn make_value() !i32 {
    return 7;
}

export fn main() i32 {
    const result: !i32 = make_value();
    const value: i32 = result.value;
    if value != 7 {
        return 1;
    }
    return 0;
}
