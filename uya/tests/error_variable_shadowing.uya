// 变量遮蔽错误测试
// 预期：编译失败（变量遮蔽被禁止）
// 注意：此文件预期编译失败，用于验证编译器禁止变量遮蔽的规则

fn main() i32 {
    var x: i32 = 10;
    
    {
        // 变量遮蔽：内层作用域不能声明与外层作用域同名的变量
        var x: i32 = 20;  // 应该编译失败（变量遮蔽）
        return x;
    }
    
    return x;
}

