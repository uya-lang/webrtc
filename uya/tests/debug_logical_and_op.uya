// 调试逻辑与运算符

use std.testing.assert_eq_i32;
use std.testing.assert_eq_bool;
use std.testing.assert_true;

test "test_logical_and_op" {
    const a: i32 = 10;
    const b: i32 = 20;
    try assert_eq_i32(a == 10 && b == 20, true);
}
