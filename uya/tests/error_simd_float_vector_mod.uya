// 向量取模 %% 仅支持整数元素类型的 @vector(T, N)
type Vec4f32 = @vector(f32, 4);

export fn main() i32 {
    const a: Vec4f32 = @vector.splat(7.0f32);
    const b: Vec4f32 = @vector.splat(3.0f32);
    const r: Vec4f32 = a % b;
    return 0;
}
