type Vec4i32 = @vector(i32, 4);
type Mask4 = @mask(4);

export fn main() i32 {
    const value: Vec4i32 = @vector.splat(3);
    const ok: Mask4 = value == @vector.splat(3);
    if @vector.all(ok) == false {
        return 1;
    }
    return 0;
}
