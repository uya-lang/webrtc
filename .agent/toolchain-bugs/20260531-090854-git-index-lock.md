## Summary
Sandboxed git metadata writes fail because `.git/index.lock` cannot be created, which blocks staging and committing otherwise validated SDP Phase 3 work.

## Affected Tasks
- 实现 SDP 行扫描和字段 parser。
- 支持 `v/o/s/t` 基础字段。
- 支持 `m=` audio/video/application。
- 支持 `a=group:BUNDLE`。
- 支持 `a=mid`。
- 支持 `a=ice-ufrag` / `a=ice-pwd`。
- 支持 `a=fingerprint`。
- 支持 `a=setup`。
- 支持 `a=rtcp-mux`。
- 支持 `a=candidate`。
- 支持 `a=rtpmap` / `a=fmtp` / `a=rtcp-fb`。
- 支持 `a=extmap`。
- 支持 Opus SDP 参数：`minptime`、`maxptime`、`useinbandfec`、`stereo`、`sprop-stereo`、`usedtx`。
- 支持 VP8 SDP 能力和反馈：`rtcp-fb` NACK、PLI、FIR、Transport-CC。
- 支持 DataChannel application section。
- 实现 SDP writer。
- 实现 JSEP validator：bundle、rtcp-mux、fingerprint、payload type、direction。
- 增加 `bench_sdp_parse`。
- 创建本轮 git 提交。

## Toolchain Command
`git add Makefile`

## Actual Error
`fatal: Unable to create '/media/winger/_dde_data/winger/uya/webrtc/.git/index.lock': Read-only file system`

## Expected Behavior
`git add` should create `.git/index.lock`, update the index, and allow a normal `git commit` for validated workspace changes.

## Repro File
`.agent/toolchain-bugs/repros/20260531-090854-git-index-lock.sh`

## Repro Code
```bash
#!/usr/bin/env bash
set -euo pipefail

cd /media/winger/_dde_data/winger/uya/webrtc
git add Makefile
```

## Notes
- Workspace file edits succeed; the failure is specific to writing git metadata under `.git/`.
- The same repository is reachable via `/home/winger/uya/webrtc` and `/media/winger/_dde_data/winger/uya/webrtc`; both paths hit the same read-only `.git` metadata failure.
- Approval policy is `never`, so there is no escalation path to retry without sandbox restrictions.
- `make test`, `make bench`, `bash tests/check_phase3_sdp.sh`, and `python3 tests/sdp_fixture_roundtrip.py` already passed before the staging attempt.
